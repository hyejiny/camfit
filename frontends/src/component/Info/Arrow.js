import React, { Component } from "react";
import "./ArrowNext.scss";

class ArrowNext extends Component {
  render() {
    const { onClick } = this.props;
    return (
 
    );
  }
}

export export default () => <FontAwesomeIcon icon={faChevronRight} />;